port: 3001
host: "0.0.0.0"
frontendUrl: "http://localhost:3000"
publicUrl: "http://localhost:3001"
sessionSecret: "change-me-in-production"
cookieName: "aiplatform_session"

oidc:
  enabled: true
  issuer: "http://localhost:8080/realms/aiplatform"
  clientId: "gateway"
  redirectUri: "http://localhost:3001/auth/callback"
  scopes: ["openid"]

keycloak:
  url: "http://localhost:8080"
  realm: "aiplatform"
  clientId: "gateway"
  adminUsername: "admin"
  adminPassword: "admin"

subsystems:
  opencode:
    baseUrl: "http://localhost:4096"
    password: "opencode"
  taskview:
    webBaseUrl: "http://localhost:5174"
    apiBaseUrl: "http://localhost:1401"
    adminUsername: "user"
    adminPassword: "user1!#Q"
    platformSsoSecret: "aiplatform-taskview-sso-secret"
  superset:
    # UI on localhost keeps iframe same-site with platform :3000; API on 127.0.0.1
    baseUrl: "http://localhost:9060"
    apiBaseUrl: "http://127.0.0.1:9068"
    adminUsername: "admin"
    adminPassword: "admin"
    platformSsoSecret: "aiplatform-superset-sso-secret"
  buildingai:
    baseUrl: "http://localhost:4091"
    apiBaseUrl: "http://127.0.0.1:4090"
    apiKey: "buildingai-api-key"
    adminUsername: "Rock"
    adminPassword: "Rock123"
    platformSsoSecret: "aiplatform-buildingai-sso-secret"

databaseUrl: "mysql://aiplatform:aiplatform@localhost:3306/aiplatform"
redisUrl: "redis://localhost:6379"
